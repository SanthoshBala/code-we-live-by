# =============================================================================
# CWLB Backend Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# Never commit .env to version control!
#
# Settings are loaded in priority order:
#   1. Environment variables (highest priority)
#   2. .env file
#   3. Default values (lowest priority)
# =============================================================================

# -----------------------------------------------------------------------------
# Application
# -----------------------------------------------------------------------------
APP_NAME="CWLB API"
DEBUG=false

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# PostgreSQL connection URL
# Format: postgresql+asyncpg://user:password@host:port/database
# For Docker (docker-compose up): use credentials from docker-compose.yml
DATABASE_URL=postgresql+asyncpg://cwlb:cwlb_dev@localhost:5432/cwlb

# -----------------------------------------------------------------------------
# CORS (comma-separated list of allowed origins)
# -----------------------------------------------------------------------------
CORS_ORIGINS=["http://localhost:3000"]

# -----------------------------------------------------------------------------
# External API Keys
# -----------------------------------------------------------------------------

# GovInfo API Key (REQUIRED for Public Law ingestion)
# Get a free key at: https://api.data.gov/signup/
# Used by: pipeline/govinfo/client.py
GOVINFO_API_KEY=your-govinfo-api-key-here

# Congress.gov API Key (REQUIRED for legislator data ingestion)
# Get a free key at: https://api.congress.gov/sign-up/
# Used by: pipeline/congress/client.py
CONGRESS_API_KEY=your-congress-api-key-here

# ProPublica API Key (for future use)
# Get a key at: https://www.propublica.org/datastore/api/propublica-congress-api
# PROPUBLICA_API_KEY=your-propublica-api-key-here
